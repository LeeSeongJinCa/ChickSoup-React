import React from 'react';

const ProfileCoverEdit = () => {
    return (
        <nav className="profile-info--cover_buttons">
            <button>프로필 사진 변경</button>
            <button>배경 사진 변경</button>
        </nav>
    )
};

export default ProfileCoverEdit;